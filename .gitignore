# Node / tooling (even though project is static, these may appear locally)
node_modules/
npm-debug.log*
yarn.lock
pnpm-lock.yaml
package-lock.json

# Logs
*.log

# macOS
.DS_Store

# VS Code
.vscode/

# Build / cache directories (if you ever add tooling)
dist/
build/
.cache/